# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
.sw?

# Xcode local user state
DerivedData/
*.xcuserstate
xcuserdata/
ios/**/.build/
ios/**/.swiftpm/


# Firebase
.firebase/
functions/lib/
functions/node_modules/
functions/firebase-tmp/
.runtimeconfig.json

# Data Connect
dataconnect/.dataconnect/
dataconnect-debug.log
src/dataconnect-generated/
src/dataconnect-admin-generated/

# Build
dist/
dist-ssr/
dist-extension/
build/
.cache/
output/
.playwright-cli/
experiments/

# Environment variables and local credential files. Only intentionally
# non-sensitive templates belong in source control.
.env
.env.*
!.env.example
!.env.*.example

# Service Account Keys & Enterprise Credentials
*-adminsdk-*.json
*-service-account*.json
*service-account*.json
*agent-key*.json
*.key.json
*credentials*.json
secrets/
credentials/
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Enterprise Agent Evaluation Datasets & Local Configs
functions/test/eval_dataset.json

# Standalone lottery engine repo (has own git)
megamillions-engine/

# Other source directories
claude-code-source-code/

# SOP Feature & Assets
src/features/sop/
public/sop-assets/
sop-assets/
*SOP*
*sop*

# Agent skills (local AI assistant config, not app code)
.agent/skills/


# Job automation tool
browser-use/

# CLI tool — the source of the published `careervivid` npm package, so it is
# tracked. Build output and deps are ignored in cli/.gitignore.
cli/node_modules/
cli/dist/

vectorlotto/

# Community content MD
community-content/

# Quest question tracker + research working files. The interview questions are
# maintained privately (synced to Firebase), so these reporting/working
# artifacts must never be pushed to the public repo.
data/quest-question-tracker.xlsx
data/quest-tracker/
data/interview-guides/_research-progress.json

# Generated mobile (iOS) question catalog — contains full question content and
# is rebuilt from source on every functions build (prebuild). Keep it private.
functions/src/mobileInterviewGuideQuestions.generated.ts

# Learning curriculum: raw third-party repos cloned by
# scripts/sync-learning-sources.mjs. Never commit these — they're large,
# have their own nested .git, and CareerVivid's own curriculum content lives
# in docs/learning/ and data/learning/ instead. The LICENSES we copy out of
# them stay tracked for compliance record-keeping.
third_party/learning-sources/*/
!third_party/learning-sources/LICENSES/
